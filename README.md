# 📝 Task Management System

This is a simple Task Management System built with Django. It allows users to create, assign, and manage tasks within an organization.

## 🚀 Features

- User authentication (login/logout)
- Superusers can:
  - Create tasks
  - Assign tasks to normal users
  - View all tasks
- Normal users can:
  - View only their assigned tasks
  - Mark their tasks as completed
- Task list with clean UI using Bootstrap 5

## 🛠️ Technologies Used

- Python 3
- Django
- Bootstrap 5 (via CDN)
- SQLite (default database)

## 📦 Setup Instructions

### 1. Clone the Repository

```bash
git clone https://github.com/your-username/task-management-system.git
cd task-management-system
